import { Box, ListItem, Flex } from "@chakra-ui/react";
import { animated } from "@react-spring/web";

export const Animated = {
  Flex: animated(Flex),
  Box: animated(Box),
  ListItem: animated(ListItem),
};
